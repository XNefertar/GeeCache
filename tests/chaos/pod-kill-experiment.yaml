apiVersion: chaos-mesh.org/v1alpha1
kind: PodChaos
metadata:
  name: geecache-pod-kill
  namespace: default
spec:
  action: pod-kill
  mode: one
  selector:
    namespaces:
      - default
    labelSelectors:
      app: geecache
  scheduler:
    cron: '@every 2m'
  duration: '30s'
